c NCS1 TRY1 CS1 NCS2 TRY2 CS2 NCS3 TRY3 CS3 NCS4 TRY4 CS4 NCS5 TRY5 CS5
AND (NCS1 AND NCS2)
AND NCS3
AND NCS4
AND NCS5
c
c 2. Mutual exclusion
AND AG NOT (CS1 AND (((CS2 OR CS3) OR CS4) OR CS5))
AND AG NOT (CS2 AND ((CS3 OR CS4) OR CS5))
AND AG NOT (CS3 AND (CS4 OR CS5))
AND AG NOT (CS4 AND CS5)
c
c 3. If one enters the try section, one is guaranteed to eventually enter the critical section
cAND AG (NOT TRY1 OR AF CS1)
cAND AG (NOT TRY2 OR AF CS2)
cAND AG (NOT TRY3 OR AF CS3)
cAND AG (NOT TRY4 OR AF CS4)
cAND AG (NOT TRY5 OR AF CS5)
c
c 4. encoding: each process is in exactly one of states {NCS,TRY,CS}
c
c 5. NCS process can enter the try section
AND AG (NOT NCS1 OR EX TRY1)
AND AG (NOT NCS2 OR EX TRY2)
AND AG (NOT NCS3 OR EX TRY3)
AND AG (NOT NCS4 OR EX TRY4)
AND AG (NOT NCS5 OR EX TRY5)
c
c 6. we ommit EXi True, since that is guaranteed in 9. for the product automaton. Basically this is similar to 5., but we do not require that eventually CSi is reached
AND AG (NOT TRY1 OR AX (CS1 OR TRY1))
AND AG (NOT TRY2 OR AX (CS2 OR TRY2))
AND AG (NOT TRY3 OR AX (CS3 OR TRY3))
AND AG (NOT TRY4 OR AX (CS4 OR TRY4))
AND AG (NOT TRY5 OR AX (CS5 OR TRY5))
c
c 7. Similar to 5.
AND AG (NOT CS1 OR EX NCS1)
AND AG (NOT CS2 OR EX NCS2)
AND AG (NOT CS3 OR EX NCS3)
AND AG (NOT CS4 OR EX NCS4)
AND AG (NOT CS5 OR EX NCS5)
c
c 8. if process one moves, process two does nothing
c
c
c
c 9. There is always a next state. We don't need that, since it is implicitly added to the formula
c AG EX True
c important! System can move when one process does not want the Mutex
AND AG (NOT NCS1 OR EX NCS1)
AND AG (NOT NCS2 OR EX NCS2)
AND AG (NOT NCS3 OR EX NCS3)
AND AG (NOT NCS4 OR EX NCS4)
AND AG (NOT NCS5 OR EX NCS5)
c
c don't go directly from NCS to CS, go via TRY.
AND AG (NCS1 -> AX NOT CS1)
AND AG (NCS2 -> AX NOT CS2)
AND AG (NCS3 -> AX NOT CS3)
AND AG (NCS4 -> AX NOT CS4)
AND AG (NCS5 -> AX NOT CS5)
c
c don't go directly from CS to TRY, go via NCS.
AND AG (CS1 -> AX NOT TRY1)
AND AG (CS2 -> AX NOT TRY2)
AND AG (CS3 -> AX NOT TRY3)
AND AG (CS4 -> AX NOT TRY4)
AND AG (CS5 -> AX NOT TRY5)
c
c
c
c
c from ASP paper
c
c Bounded Overtaking: While Process P_i is in the waiting section, every other process will exit its critical section at most once
c AND AG NOT XXX
c XXX = ((TRY1 AND CS2) AND (TRY1 EU YYY))
c YYY = ((TRY1 AND TRY2) AND (TRY1 EU (TRY1 AND CS2)))
cAND AG NOT ((TRY1 AND CS2) AND (TRY1 EU ((TRY1 AND TRY2) AND (TRY1 EU (TRY1 AND CS2)))))
cAND AG NOT ((TRY1 AND CS3) AND (TRY1 EU ((TRY1 AND TRY3) AND (TRY1 EU (TRY1 AND CS3)))))
cAND AG NOT ((TRY2 AND CS1) AND (TRY2 EU ((TRY2 AND TRY1) AND (TRY2 EU (TRY2 AND CS1)))))
cAND AG NOT ((TRY2 AND CS3) AND (TRY2 EU ((TRY2 AND TRY3) AND (TRY2 EU (TRY2 AND CS3)))))
cAND AG NOT ((TRY3 AND CS1) AND (TRY3 EU ((TRY3 AND TRY1) AND (TRY3 EU (TRY3 AND CS1)))))
cAND AG NOT ((TRY3 AND CS2) AND (TRY3 EU ((TRY3 AND TRY2) AND (TRY3 EU (TRY3 AND CS2)))))
c
c
c Maximal Reactivity: if process P_i is in the waiting section and all others are in the noncritical section, then the there's a next state such that P_i is in the critical section
cAND AG (((TRY1 AND NCS2) AND NCS3) -> EX CS1)
cAND AG (((TRY2 AND NCS1) AND NCS3) -> EX CS2)
cAND AG (((TRY3 AND NCS1) AND NCS2) -> EX CS3)
